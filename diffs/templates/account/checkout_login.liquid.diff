REPLACE_BLOCK
{% form 'create_checkout_account', context_name: 'f' %}
ANY
{% endform %}
WITH
{% form 'checkout_registration' %}
  {% include 'registration_form', form: form %}
  <input type="submit" value="create account" class="btn custom_btn custom-button btn-tradewind"/>
{% endform %}
END

REPLACE
cart_options.allow_guest_checkout
WITH
shop.guest_checkout_allowed?
END

REPLACE
{% form 'guest_checkout', context_name: 'f' %}
WITH
{% form 'guest_checkout' %}
END

REPLACE
{% form_field 'f.input', 'customer_email', { "label": "email address", "required": true } %}
WITH
<div class="input string required">
  <label class="string required control-label" for="customer_email"><abbr title="required">*</abbr> Email address</label>
  <input class="string required" id="customer_email" name="guest[email]" type="text" value="{{ form.email }}">

  {% if form.errors contains 'email' %}
    <span class="error">{{ form.errors['email'] }}</span>
  {% endif %}
</div>
END

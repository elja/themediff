REPLACE
<div class="line-items-reflective-view">
WITH
{% form 'populate_product' %}
  <div class="line-items-reflective-view">
END

REPLACE
{% form 'populate_product', context_name: 'f' %}
{% include 'line_item_hidden_fields' %}
WITH
END

REPLACE
{% unless product.has_multiple_quantity_options? %}
  {% include 'stock_message', line_item: line_items[0], index: 0 %}
{% endunless %}
WITH
<span class="stock" id="stock"></span>
END

REPLACE
{% if line_items.size == 1 %}
WITH
{% if product.product_options_enabled? == false or product.options_or_groups.multiple_quantity == empty %}
END

REPLACE
<input type="text" data-id="0" data-type="quantity" class="amount" value="{{ line_items[0].quantity }}">
WITH
<input type="text" data-type="quantity" class="amount" name="product[quantity]" value="{{ form.quantity | default: 0 }}"/>
END


REPLACE
{% include 'product_summary', product: product, line_items: line_items %}
WITH
{% include 'product_summary' %}
END

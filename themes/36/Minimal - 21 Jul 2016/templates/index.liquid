{% include 'page' with home_page %}

	<script>
	 /* $(function() { 
		 $('.product .modal').dialog({ autoOpen: false, modal: true });
		 $('a.quick-view').on('click', function(event){
			 var dialogId = $(this).parents('.product').data('quick-view-modal'); 
			 $('#' + dialogId).dialog('open');
			 return event.preventDefault();
		 });
	 }); */
	 $(function(){
	 	$('a.quick-view').on('click', function(e) {
			$('.modal').css('visibility', 'visible');
			return e.preventDefault();
		});
	 });
	</script>